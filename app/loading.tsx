import PageLoadingScreen from "@/components/PageLoadingScreen"

/** Shown for every page while it loads (first load + any navigation). */
export default function Loading() {
  return <PageLoadingScreen />
}
